# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api\query-qris\query-qris.contract.spec.ts >> Query QRIS contract >> QRY-CON-001 - validasi response header / validate response header
- Location: tests\api\query-qris\query-qris.contract.spec.ts:9:7

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

# Test source

```ts
  1   | import { expect, test } from '../../fixtures/api.fixture';
  2   | 
  3   | import { reportTitle } from '../../../src/shared/utils/report-language';
  4   | import { isValidSnapTimestamp } from '../../../src/shared/utils/timestamp';
  5   | import { attachQueryApiExchange, assertQuerySuccessExchange, getHeader } from './helpers';
  6   | import { createDynamicTransactionForQuery } from './setup';
  7   | 
  8   | test.describe('Query QRIS contract', () => {
  9   |   test(
  10  |     reportTitle('QRY-CON-001', 'validasi response header', 'validate response header'),
  11  |     async ({ generateQrisDynamicClient, queryQrisClient, dynamicEnv }, testInfo) => {
  12  |       const body = await createDynamicTransactionForQuery({
  13  |         generateQrisDynamicClient,
  14  |         queryQrisClient,
  15  |         testInfo,
  16  |         testCaseId: 'QRY-CON-001'
  17  |       });
  18  |       const exchange = await queryQrisClient.queryQris({ body });
  19  | 
  20  |       await attachQueryApiExchange(testInfo, 'QRY-CON-001', exchange);
  21  |       expect(getHeader(exchange.response.headers, 'content-type')).toContain('application/json');
> 22  |       expect(isValidSnapTimestamp(getHeader(exchange.response.headers, 'x-timestamp'))).toBe(true);
      |                                                                                         ^ Error: expect(received).toBe(expected) // Object.is equality
  23  |       assertQuerySuccessExchange(exchange, dynamicEnv);
  24  |     }
  25  |   );
  26  | 
  27  |   test(
  28  |     reportTitle('QRY-CON-002', 'validasi responseCode 2005100', 'validate responseCode 2005100'),
  29  |     async ({ generateQrisDynamicClient, queryQrisClient, dynamicEnv }, testInfo) => {
  30  |       const body = await createDynamicTransactionForQuery({
  31  |         generateQrisDynamicClient,
  32  |         queryQrisClient,
  33  |         testInfo,
  34  |         testCaseId: 'QRY-CON-002'
  35  |       });
  36  |       const exchange = await queryQrisClient.queryQris({ body });
  37  |       const responseBody = exchange.response.body as Record<string, unknown>;
  38  | 
  39  |       await attachQueryApiExchange(testInfo, 'QRY-CON-002', exchange);
  40  |       expect(responseBody.responseCode).toBe('2005100');
  41  |       assertQuerySuccessExchange(exchange, dynamicEnv);
  42  |     }
  43  |   );
  44  | 
  45  |   test(
  46  |     reportTitle(
  47  |       'QRY-CON-003',
  48  |       'latestTransactionStatus sesuai daftar TSD',
  49  |       'latestTransactionStatus follows TSD list'
  50  |     ),
  51  |     async ({ generateQrisDynamicClient, queryQrisClient, dynamicEnv }, testInfo) => {
  52  |       const body = await createDynamicTransactionForQuery({
  53  |         generateQrisDynamicClient,
  54  |         queryQrisClient,
  55  |         testInfo,
  56  |         testCaseId: 'QRY-CON-003'
  57  |       });
  58  |       const exchange = await queryQrisClient.queryQris({ body });
  59  | 
  60  |       await attachQueryApiExchange(testInfo, 'QRY-CON-003', exchange);
  61  |       assertQuerySuccessExchange(exchange, dynamicEnv);
  62  |     }
  63  |   );
  64  | 
  65  |   test(
  66  |     reportTitle(
  67  |       'QRY-CON-004',
  68  |       'amount response memiliki value dan currency',
  69  |       'response amount has value and currency'
  70  |     ),
  71  |     async ({ generateQrisDynamicClient, queryQrisClient, dynamicEnv }, testInfo) => {
  72  |       const body = await createDynamicTransactionForQuery({
  73  |         generateQrisDynamicClient,
  74  |         queryQrisClient,
  75  |         testInfo,
  76  |         testCaseId: 'QRY-CON-004'
  77  |       });
  78  |       const exchange = await queryQrisClient.queryQris({ body });
  79  | 
  80  |       await attachQueryApiExchange(testInfo, 'QRY-CON-004', exchange);
  81  |       assertQuerySuccessExchange(exchange, dynamicEnv);
  82  |     }
  83  |   );
  84  | 
  85  |   test(
  86  |     reportTitle(
  87  |       'QRY-CON-005',
  88  |       'response time berada dalam API_MAX_RESPONSE_MS',
  89  |       'response time is within API_MAX_RESPONSE_MS'
  90  |     ),
  91  |     async ({ generateQrisDynamicClient, queryQrisClient, dynamicEnv }, testInfo) => {
  92  |       const body = await createDynamicTransactionForQuery({
  93  |         generateQrisDynamicClient,
  94  |         queryQrisClient,
  95  |         testInfo,
  96  |         testCaseId: 'QRY-CON-005'
  97  |       });
  98  |       const exchange = await queryQrisClient.queryQris({ body });
  99  | 
  100 |       await attachQueryApiExchange(testInfo, 'QRY-CON-005', exchange);
  101 |       expect(exchange.response.responseTimeMs).toBeLessThanOrEqual(dynamicEnv.apiMaxResponseMs);
  102 |       assertQuerySuccessExchange(exchange, dynamicEnv);
  103 |     }
  104 |   );
  105 | });
  106 | 
```